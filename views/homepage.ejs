<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anime Watchlist</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <%- include("header") %>
    <div class="homepageBody">
        <!-- <nav class="genreList">
            <a href="/">All</a>
        </nav> -->
        <div class="homepageList">
            <% rows.forEach( (row)=> {%>
                <div class="homepageListElement">
                    <a href="/anime/<%= row.id %>"><div class="img-wrap">
                        <img src="<%= row.image_url %>" alt="Image" />
                        </div></a>
                    <div class="homepageTitle"><%= row.title %></div>
                    <div class="homepageEpisodesWatched"><%= row.episodeswatched %> / <%= row.episodes %> Episodes Watched</div>
                </div>
            <% }) %>
        </div>
    </div>
     <%- include("footer") %>
</body>
</html>